# 需求分析文档 — 万能视频下载网站

> 本文档沉淀项目的需求背景、目标用户、功能范围与边界，供后续扩展功能时为 AI / 团队提供参考。

## 1. 背景与动机

很多用户有"下载并保存视频到本地"的需求，但常遇到以下痛点：

- 部分平台不支持下载功能；
- 平台自带下载不够方便（不支持批量、有清晰度限制、有水印、需要会员等）；
- 缺少一个"随时随地、跨平台、手机也能用"的统一入口。

因此需要开发一个**万能视频下载网站**，让用户能够更快速、随时随地、从各个平台下载视频。

## 2. 项目目标

- 提供一个 **Web 端**（桌面 + 移动端浏览器均可用）的视频下载工具。
- **全平台覆盖**：尽可能支持主流平台（YouTube、Bilibili、抖音/TikTok、X/Twitter、Instagram 等）。
- **简单易用**：粘贴链接 → 选择清晰度 → 一键下载。
- **商业化定位**：界面精致、突出实用价值，引导用户付费（本期定价区块为视觉展示）。
- **轻量、低风险**：站在开源巨人肩膀上（yt-dlp），尽量少自研下载逻辑。

## 3. 目标用户

- 需要保存视频用于学习、二次创作、离线观看的普通用户；
- 自媒体 / 内容创作者（关注无水印、高清、批量）；
- 移动端用户（手机上也要能下载）。

## 4. 功能范围（本期 MVP）

### 4.1 核心功能（本期实现）

| 功能 | 说明 |
| --- | --- |
| 视频信息解析 | 输入链接，返回标题、封面、时长、作者、可选清晰度列表 |
| 清晰度选择 | 1080p / 720p / 480p / **最佳画质（标注具体分辨率如 1080p/4K）** / 仅音频(MP3) 等 |
| 视频下载 | 服务端下载并合并（高清视频流 + 音频流），再提供给用户 |
| 下载进度 | 实时展示百分比、速度、预计剩余时间 |
| 文件下载 | 完成后浏览器触发下载，临时文件随后清理 |
| 商业化 UI | 浅色蓝调商业风格，品牌名 **VeloClip**，含定价区块（视觉展示）、FAQ、免责声明 |
| 响应式 | 桌面与移动端浏览器均可正常使用 |

### 4.2 AI 分析（2026-06 已实现）

| 功能 | 说明 |
| --- | --- |
| 视频解析内嵌 AI | 解析成功后同一卡片内提供 AI 分析，无需二次输入链接 |
| AI 总结 | DeepSeek 基于字幕生成结构化 Markdown 总结 |
| 思维导图 | 自动生成大纲树，支持缩放/平移/导出 PNG |
| 字幕/转录 | 带时间戳的字幕列表（平台官方字幕，B 站无 CC 时兜底弹幕） |
| AI 提问 | 基于字幕内容的多轮问答 |

> 配置：后端 `backend/.env` 中设置 `DEEPSEEK_API_KEY`；模型推荐 `deepseek-chat`。

### 4.3 非功能需求

- **轻量无数据库**：任务进度用内存管理，临时文件用完即清理。
- **技术栈以 Python 为核心**（下载能力），前端为 React 工程。
- **最小改动**：直接封装 yt-dlp 的 Python API，不修改开源源码。
- **友好错误提示**：无效链接 / 不支持站点 / 解析失败等返回中文提示。

## 5. 不在本期范围（后续可扩展）

- 真实**支付/会员/账号体系**（需要数据库与登录，违背"无数据库、轻量"设定）。
- **AI 视频总结、字幕翻译**（总结/思维导图/问答已实现；字幕翻译待扩展）。
- **批量下载队列、历史记录持久化**（需数据库）。
- 需要登录态 / 会员 / 受 DRM 保护的内容下载（yt-dlp 能力边界外）。

## 6. 约束与风险

- 部分平台需登录、会员或受 DRM 保护的内容**无法下载**，属预期范围外。
- 高清下载依赖 **ffmpeg** 做音视频合并：本项目用 `imageio-ffmpeg` 自带二进制规避手动安装。
- 服务端下载会占用服务器带宽与临时存储，需做**临时文件清理**。
- **合规性**：仅供个人合规使用，前端需展示**免责声明**。

## 7. 验收标准

- 输入主流平台公开视频链接，可成功解析出信息与清晰度。
- 选择清晰度后可完整下载，文件可正常播放（音视频合并正确）。
- 进度条实时更新，完成后可触发浏览器下载。
- 桌面与移动视口下 UI 表现正常、风格符合商业化定位。

## 8. 平台兼容性现状（持续更新，2026-06-12）

> 核心目标：尽量"免登录、免 Cookie"下载主流平台。下表记录各平台的实测结论，
> 详细技术根因与解决方案见《方案设计》第 10 节。

| 平台 | 现状 | 说明 |
| --- | --- | --- |
| 直链 MP4 / HLS | ✅ 已通过 | 实测：`sample-5s.mp4` 解析+下载，ffprobe 校验 h264+aac 1080p 正常 |
| 抖音 Douyin | ✅ 已解决并实测 | 关键是 URL 规范化：`jingxuan?modal_id=ID` 等链接 yt-dlp 不识别，需转为 `/video/{id}`；规范化后**免 Cookie** 解析下载。实测 720p hevc+aac 合并正常 |
| TikTok | ✅ 实测可用 | 免 Cookie。实测 1080p/720p hevc+aac 合并正常 |
| 哔哩哔哩 Bilibili | ✅ 已解决并实测 | **免 Cookie 打通**（解析→下载→合并→可播放，ffprobe 校验 av1+aac 正常）。根因不是 UA，而是：① yt-dlp 把已签名的 playurl `query` 二次编码破坏 WBI 签名；② Referer 指向被风控的 `/video/` 页面会 412。运行时 monkeypatch 已修复，详见《方案设计》10.2。免登录最高约 480p/720p（更高码率需会员） |
| YouTube | ⛔ 本期不支持（已知限制） | "确认你不是机器人"人机验证（IP 信誉级），免 Cookie 无法绕过。已保留 Cookie 兜底与友好中文提示；本轮不投入攻坚 |
| 西瓜 Ixigua | ⚠️ 需 Cookie | 已加 URL 规范化（`m.ixigua.com/dx/{id}` 等 → `www.ixigua.com/{id}/`），提取器明确要求 Cookie（即便非登录态）。放置 `cookies/ixigua.txt` 后可用 |
| X / Twitter | ⏳ 待验证 | 普通公开视频推文（`/status/{id}`）提取器路径存在；直播 broadcast 链接会慢/超时（已加 `socket_timeout` 防挂起）。需有效公开普通视频推文复验 |
| 快手 / 微博 / Instagram | ⚠️ 多需 Cookie | 快手/微博网页取流需携带 Cookie；Instagram 多需登录。已加快手短链跟随与 cookie 友好提示；Cookie 基础设施就绪，放置对应 `cookies/<平台>.txt` 后可用 |

### 重要决策：尽量不依赖 Cookie

用户明确反馈"使用 Cookie 方式非常不便"。因此**优先采用免 Cookie 的技术手段**
（URL 规范化、开放 API 绕过、浏览器 UA、指纹伪装等）。Cookie 仅作为兜底能力保留
（代码已支持 `backend/cookies/<平台>.txt` 与环境变量），不作为主路径。

## 9. 迭代记录（2026-06-13 ~ 2026-06-14）

> 详细技术方案见《方案设计》第 12、13 节。

### 9.1 已完成

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| 链接输入框「全选」按钮 | ✅ 已实现 | 与「粘贴」并列，`inputRef` + `handleSelectAll` |
| 会员定价布局优化 | ✅ 已实现 | `sm:grid-cols-3` 三档横排紧凑布局；CTA 跳转 `#download` |
| 网页嵌入视频识别 | ✅ 已实现 | `resolve_video_url()` 从课程/文章页 HTML 提取 B 站等平台直链；响应含 `resolved_url` |
| 链接格式规范化 | ✅ 已实现 | B 站无 www、抖音 modal_id、短链跟随等；支持无 `https://` 前缀 |
| 品牌英文名 VeloClip | ✅ 已实现 | 导航 Logo、页脚、浏览器标题 `VeloClip · 万能视频下载器` |
| 最佳画质标注分辨率 | ✅ 已实现 | `_best_quality_label()` 显示如「最佳画质 1080p」「最佳画质 4K」，取代「（自动）」 |
| 爱奇艺 Cookie 映射（服务端） | ✅ 已实现 | `PLATFORM_COOKIE_MAP` 含 iqiyi.com / pps.tv，仅运维兜底，无用户侧配置 |

### 9.2 明确不做

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| Cookie 网页上传配置 | ❌ 已取消 | 用户反馈 Cookie 配置不便；产品路径为**粘贴链接即可解析**，Cookie 仅保留服务端 env/文件兜底 |

### 9.3 实测补充

| 场景 | 结论 |
| --- | --- |
| 编程导航课程页（嵌入 B 站） | ✅ 粘贴 `codefather.cn/course/.../section/...` 可识别内嵌 BV 号并解析 |
| 后端未重启 | ⚠️ 代码更新后须重启 uvicorn，否则仍返回旧文案（如「最佳画质（自动）」） |

## 10. 已实现体验优化（2026-06-13）

| 功能 | 状态 |
| --- | --- |
| 多任务下载列表可见 | ✅ 已实现 |
| 下载取消 | ✅ 已实现（`POST /api/cancel/{task_id}`） |
| 进度条单调递增（防闪跳） | ✅ 已实现 |
| MP4 faststart + 优先 h264 | ✅ 已实现 |
| 保存弹窗仅触发一次 | ✅ 已实现 |

## 11. AI 分析与体验优化（2026-06-18）

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| DeepSeek AI 分析 API | ✅ 已实现 | `/api/ai/analyze`、`/api/ai/transcript`、`/api/ai/chat` |
| 解析页内嵌 AI 区块 | ✅ 已实现 | 与下载共用一次解析，AI 面板在卡片下方展开 |
| 思维导图缩放/导出 PNG | ✅ 已实现 | 滚轮缩放、拖拽平移；离屏克隆导出避免边框压字 |
| 清晰度短边像素 | ✅ 已实现 | 横/竖屏统一按短边标注 720p/1080p；最佳画质优先最高码流 |
| B 站弹幕字幕兜底 | ✅ 已实现 | CC 失败时回退 danmaku；界面标注来源 |
| 无字幕元数据降级 | ✅ 已实现 | X 等平台无字幕时 `subtitle_source=metadata`，AI 仍可用 |
| 思维导图全屏 | ✅ 已实现 | 浏览器 Fullscreen API，Esc 退出 |
| B 站 CC 优先写入 | ✅ 已实现 | 固定短路径、UTF-8、空文件过滤、结构化日志 |

### 11.1 AI 字幕提取修复（2026-06-20）

| 问题 | 根因 | 方案 |
| --- | --- | --- |
| Windows 报 `[Errno 22] Invalid argument` | 字幕下载复用 `_base_opts` 携带旧版 ffmpeg；CC 字幕 `vtt/srt/best` 转码在 Windows 触发 EINVAL；或 8000 端口旧 uvicorn 未退出仍在跑旧代码 | `transcript._subtitle_opts()`：固定短路径 outtmpl、跳过 ffmpeg、启用 `windowsfilenames`；B 站 AI 优先弹幕；CC 失败回退弹幕；友好中文错误 |
| 抖音有画面字幕但 AI 不可用 | 公开 `aweme/detail` API 不返回软字幕轨，多为硬字幕 | 专用错误文案说明平台限制 |
| 改了代码仍报旧错 | 8000 被僵尸 uvicorn 占用，新进程绑定失败 | 改完重启后端，任务管理器结束旧 python/uvicorn |

**实测**：`BV1awjw6qEog` 弹幕提取 + DeepSeek 分析正常。

### 11.2 字幕写入容错与无字幕降级（2026-06-22）

| 问题 | 方案 |
| --- | --- |
| B 站字幕文件写入失败 | 固定 `tmp/video` 短路径；写入前 `mkdir`；UTF-8 读取；跳过空文件；CC 优先、失败回退弹幕；结构化日志 |
| X 等平台无字幕导致 AI 422 | `extract_for_analysis()` 降级为元数据（标题/简介/作者/标签）；`subtitle_source=metadata` |
| 抖音无软字幕轨 | 仍阻断并返回专用说明（硬字幕限制） |
| 开发环境解析打到旧代码 | 开发端口改 **8001**；`GET /health` 返回 `pid` 校验进程 |
| 思维导图查看空间不足 | 思维导图 Tab 增加全屏按钮 |

**验收**：`python -m unittest tests.test_transcript`；B 站 `BV1dD42137cz` 字幕提取；X 无字幕 URL 走 metadata 分析。

### 11.3 定价区与文案优化（2026-06-22）

| 改动 | 说明 |
| --- | --- |
| 定价三档横排 | `sm:grid-cols-3`、缩小 padding/字号，三卡同排更紧凑 |
| CTA 跳转 | 「立即免费下载」及定价按钮指向 `#download`（Hero 下载区） |
| 文案修正 | 「高清无水印」改为「高清多档位」；FAQ 水印说明改为依平台而定 |
| 定价说明 | 明确 Pro/旗舰为规划展示，标 * 限流项尚未接入 |
